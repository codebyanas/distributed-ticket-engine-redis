model Event {
	id        String   @id @default(cuid())
	name      String
	startsAt  DateTime
	seats     Seat[]
	createdAt DateTime @default(now())
	updatedAt DateTime @updatedAt

	@@index([startsAt])
}
